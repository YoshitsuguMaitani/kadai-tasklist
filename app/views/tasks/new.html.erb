<h1>タスクの新規作成ページ</h1>

<!--form_withで始まり、endで終わる。これはformタグと同様の役割だと考える。-->
<!--local: true を抜かした場合、画面遷移しない通信になる。外部サイトへの通信に効果を発揮する。-->
<%= form_with(model: @task, local: true) do |f| %>
<!--フォームの入力欄の設定-->
 <%= f.label :content, "タスク" %>
 <%= f.text_field :content %>
<!--送信ボタンの生成-->
 <%= f.submit "投稿" %>
<% end %>

<%= link_to "一覧に戻る", tasks_path %>
<h1>タスク一覧</h1>

<ul>
    <!--Controllerから渡された@tasksのレコード群を1つずつtaskとして取り出しcontentカラムの値に代入-->
    <% @tasks.each do |task| %>
     <li><%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %></li>
    <% end %>
</ul>
<%= paginate @tasks %>
<%= link_to "新規タスクの投稿" , new_task_path %>